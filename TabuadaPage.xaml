<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tabuada.TabuadaPage"
             Title="Calcular Tabuada">
    <ScrollView>
        <VerticalStackLayout>
            <Entry 
                   x:Name="Numero"
                   Placeholder="Informe um número"
                   Keyboard="Numeric"
                   Margin="10" />

            

            <VerticalStackLayout x:Name="vsTabuada">
                
            </VerticalStackLayout>
            <Button
                    x:Name="btExecutar"
                    Text="Resultado"
                    Margin="10" 
                    Clicked="btExecutar_Clicked"/>
    </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>